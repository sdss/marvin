version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - web
      pip_install_args:
        - "-c requirements.txt"

sphinx:
  configuration: docs/sphinx/conf.py