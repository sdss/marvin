# This configuration file is for Flask running on any utah.sdss.edu machine.

# NOTE: Only all uppcase values are read!
# Ref: http://flask.pocoo.org/docs/api/#flask.Config

# MODULES_INIT_SCRIPT = /usr/share/Modules/init/python.py

# log raw SQL queries
LOG_SQL_QUERIES = True

# leave as default (localhost:5000)
# SERVER_NAME = 'localhost:5000'
SECRET_KEY = 'MARVIN_5000'
WTF_CSRF_ENABLED = True
WTF_CSRF_SECRET_KEY = 'wtfsecretkey'

# Parameters for File Uploads  
UPLOAD_FOLDER = '/tmp/'
ALLOWED_EXTENSIONS = set(['txt','csv'])
MAX_CONTENT_LENGTH = 50*1024*1024

# PREFERRED_URL_SCHEME = 'https'

# ------------------------------
# Database connection parameters
# ------------------------------

DB_DATABASE = 'manga'
DB_HOST = 'localhost'
DB_USER = ''
DB_PASSWORD = ''
DB_PORT = 5432

FEATURE_FLAGS_NEW = True